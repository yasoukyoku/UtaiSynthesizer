# Training sidecar venv (training/.venv). Dev machine: python 3.10.
# torch is installed separately with the CUDA index (release-time env configurator
# will own this step; see project memory "torch 环境配置" backlog):
#   python -m pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
numpy<2
scipy
librosa>=0.10
soundfile
scikit-learn
tensorboard
matplotlib
tqdm
onnxruntime
pyyaml
